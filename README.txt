Data store
----------

Flexible data store which support queries which do not match the pre-defined
fields in the table.

Tables need not be defined beforehand. Query parser will adapt itself to the
query elements rather than fail.

